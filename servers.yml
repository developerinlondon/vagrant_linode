---
live:
  web.sds:
    distribution: 'Ubuntu 16.04 LTS'
#    imageid: 1667220
    datacenter: frankfurt
    plan: 'Linode 1024'
    private_key_path: .ssh/id_rsa
    synced_folders:
    - src: .
      dest: /vagrant
      disabled: true
    - src: saltstack
      dest: /saltstack
      disabled: false
dev:
  n:
    #distribution: 'Ubuntu 16.04 LTS'
    imageid: 1667220
    datacenter: frankfurt
    plan: 'Linode 1024'
    private_key_path: .ssh/id_rsa
    synced_folders:
    - src: .
      dest: /vagrant
      disabled: true
    - src: saltstack
      dest: /srv
      disabled: false
image:
  salt:
    distribution: 'Ubuntu 16.04 LTS'
    #imageid: 1667220
    datacenter: frankfurt
    plan: 'Linode 1024'
    private_key_path: .ssh/id_rsa
    synced_folders:
    - src: .
      dest: /vagrant
      disabled: true
    - src: saltstack
      dest: /srv
      disabled: false
